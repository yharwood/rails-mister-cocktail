<div class="container mt-5">
  <h1>New</h1>
  <p><%= simple_form_for @cocktail do |f| %>
      <%= f.input :name %>
       <%= f.input :photo, as: :file %>
        <%= f.association :doses %>
      <%= f.submit 'Save New Cocktail', class: 'btn btn-primary mt-2' %>
    <% end %>
  </p>
</div>
